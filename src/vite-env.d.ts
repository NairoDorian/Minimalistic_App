/// <reference types="vite/client" />

// Application version string injected by Vite's `define` at build time
// (see vite.config.ts). Single source of truth: package.json.
declare const __APP_VERSION__: string;
