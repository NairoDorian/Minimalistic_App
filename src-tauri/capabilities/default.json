{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the minimalistic app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "autostart:default",
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled",
    "store:default",
    "updater:default",
    "process:default"
  ]
}
